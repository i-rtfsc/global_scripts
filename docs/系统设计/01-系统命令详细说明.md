# Global Scripts V3 系统命令详细说明

## 📋 概述

本文档详细说明Global Scripts V3的核心系统命令，包括版本查询、状态检查、插件管理等功能，为用户提供完整的系统管理能力。

## 🔧 核心系统命令

### 1. gs-version - 版本信息管理

#### 功能描述
`gs-version`命令提供全面的版本信息查询功能，包括系统版本、组件版本、依赖信息等。

#### 使用语法
```bash
gs-version [选项]
```

#### 支持选项
```bash
# 基本选项
gs-version                    # 显示完整版本信息
gs-version --short           # 显示简短版本号
gs-version --json            # JSON格式输出
gs-version --components      # 显示组件版本信息
gs-version --dependencies    # 显示依赖版本信息

# 高级选项
gs-version --build-info      # 显示构建信息
gs-version --git-info        # 显示Git信息（如果可用）
gs-version --system-info     # 显示系统环境信息
```

#### 输出示例
```bash
# 基本版本信息
$ gs-version
Global Scripts V3 - 版本信息
============================
版本: 3.0.0
构建时间: 2025-05-18 10:30:15
Git提交: a1b2c3d (如果可用)
架构: .meta+函数检测架构
Shell: bash 4.4.20 (x86_64-apple-darwin)
系统: macOS 12.6.0

# JSON格式输出
$ gs-version --json
{
  "version": "3.0.0",
  "build_time": "2025-05-18T10:30:15Z",
  "git_commit": "a1b2c3d",
  "architecture": "meta-function-detection",
  "shell": {
    "type": "bash",
    "version": "4.4.20",
    "platform": "x86_64-apple-darwin"
  },
  "system": {
    "os": "macOS",
    "version": "12.6.0"
  },
  "components": {
    "core": "3.0.0",
    "plugins": "3.0.0",
    "tools": "3.0.0"
  }
}

# 简短版本
$ gs-version --short
3.0.0
```

#### 实现要点
- 从VERSION文件或Git tag读取版本号
- 动态检测Shell类型和版本
- 支持多种输出格式
- 提供构建和环境信息

---

### 2. gs-status - 系统状态检查

#### 功能描述
`gs-status`命令提供全面的系统状态检查，包括安装状态、配置信息、性能指标、插件状态等。

#### 使用语法
```bash
gs-status [选项] [检查项]
```

#### 支持选项
```bash
# 基本选项
gs-status                    # 显示完整状态信息
gs-status --brief           # 显示简要状态
gs-status --json            # JSON格式输出
gs-status --health          # 健康检查模式

# 特定检查项
gs-status system            # 系统环境状态
gs-status config            # 配置文件状态
gs-status plugins           # 插件状态概览
gs-status performance       # 性能指标
gs-status cache             # 缓存系统状态
gs-status logs              # 日志状态

# 高级选项
gs-status --verbose         # 详细输出模式
gs-status --diagnostic      # 诊断模式
gs-status --fix-issues      # 尝试自动修复问题
```

#### 输出示例
```bash
# 完整状态信息
$ gs-status
Global Scripts V3 - 系统状态
============================

📊 总体状态: ✅ 正常运行
📍 安装路径: /Users/user/.globalscripts
🕒 最后启动: 2025-05-18 14:30:25
⏱️  启动时间: 8ms

🔧 系统环境:
  操作系统: macOS 12.6.0
  Shell环境: bash 4.4.20
  Python版本: 3.9.7 (可选依赖)

⚙️  配置文件:
  默认配置: ✅ ~/.globalscripts/config/default.meta
  用户配置: ✅ ~/.globalscripts/config/user.meta
  企业配置: ❌ 未配置

🧩 插件状态:
  已安装插件: 5个
  已启用插件: 3个 (android, git, system)
  已禁用插件: 2个 (themes, tools)
  插件错误: 0个

💾 缓存系统:
  L1缓存: ✅ 正常 (85% 命中率)
  L2缓存: ✅ 正常 (12MB, 90% 命中率) 
  缓存清理: 上次清理 2小时前

📈 性能指标:
  启动时间: 8ms (优秀)
  内存占用: 6.2MB (良好)
  命令响应: 平均 2ms (优秀)

📝 日志状态:
  系统日志: 125KB (正常)
  错误日志: 0KB (无错误)
  性能日志: 45KB (正常)

# 简要状态
$ gs-status --brief
✅ Global Scripts V3 运行正常
📍 /Users/user/.globalscripts | ⏱️ 8ms | 🧩 3/5插件 | 💾 85%缓存

# JSON格式
$ gs-status --json
{
  "status": "healthy",
  "installation_path": "/Users/user/.globalscripts",
  "last_startup": "2025-05-18T14:30:25Z",
  "startup_time_ms": 8,
  "system": {
    "os": "macOS 12.6.0",
    "shell": "bash 4.4.20",
    "python": "3.9.7"
  },
  "config_files": {
    "default": {"status": "ok", "path": "~/.globalscripts/config/default.meta"},
    "user": {"status": "ok", "path": "~/.globalscripts/config/user.meta"},
    "enterprise": {"status": "missing"}
  },
  "plugins": {
    "total": 5,
    "enabled": 3,
    "disabled": 2,
    "errors": 0,
    "list": ["android", "git", "system", "themes", "tools"]
  },
  "cache": {
    "l1_hit_rate": 0.85,
    "l2_hit_rate": 0.90,
    "l2_size_mb": 12,
    "last_cleanup": "2h ago"
  },
  "performance": {
    "startup_time_ms": 8,
    "memory_usage_mb": 6.2,
    "avg_command_response_ms": 2
  },
  "logs": {
    "system_log_kb": 125,
    "error_log_kb": 0,
    "performance_log_kb": 45
  }
}

# 健康检查模式
$ gs-status --health
🏥 Global Scripts V3 健康检查
=============================
✅ 核心文件完整
✅ 配置文件有效
✅ 插件系统正常
✅ 缓存系统健康
✅ 性能指标良好
✅ 无错误或警告

总体健康度: 100% (优秀)
```

#### 实现要点
- 检查核心文件完整性
- 验证配置文件有效性
- 监控性能指标
- 统计插件状态
- 分析缓存健康度
- 支持自动修复建议

---

### 3. gs-plugins - 插件管理系统

#### 功能描述
`gs-plugins`命令提供完整的插件管理功能，包括插件列表、状态查询、启用/禁用、重新加载等操作。

#### 使用语法
```bash
gs-plugins <子命令> [选项] [参数]
```

#### 支持的子命令

##### 3.1 gs-plugins list - 显示插件列表
```bash
# 基本用法
gs-plugins list                    # 显示所有插件
gs-plugins list --enabled         # 只显示已启用插件
gs-plugins list --disabled        # 只显示已禁用插件
gs-plugins list --available       # 显示可用插件
gs-plugins list --json            # JSON格式输出

# 输出示例
$ gs-plugins list
Global Scripts V3 - 插件列表
============================

✅ 已启用插件 (3个):
┌──────────┬─────────┬──────────────────┬────────────┬──────────┐
│ 插件名称 │ 版本    │ 描述             │ 命令数量   │ 状态     │
├──────────┼─────────┼──────────────────┼────────────┼──────────┤
│ android  │ 3.0.0   │ Android开发工具集│ 12         │ 正常     │
│ git      │ 3.0.0   │ Git工作流工具    │ 8          │ 正常     │
│ system   │ 3.0.0   │ 系统管理工具     │ 6          │ 正常     │
└──────────┴─────────┴──────────────────┴────────────┴──────────┘

❌ 已禁用插件 (2个):
┌──────────┬─────────┬────────────────┬────────────┬──────────┐
│ 插件名称 │ 版本    │ 描述           │ 命令数量   │ 状态     │
├──────────┼─────────┼────────────────┼────────────┼──────────┤
│ themes   │ 3.0.0   │ 主题配置工具   │ 4          │ 已禁用   │
│ tools    │ 3.0.0   │ 开发工具集     │ 10         │ 已禁用   │
└──────────┴─────────┴────────────────┴────────────┴──────────┘

📊 统计信息:
总插件数: 5个 | 已启用: 3个 | 已禁用: 2个 | 总命令数: 26个

$ gs-plugins list --enabled --json
{
  "enabled_plugins": [
    {
      "name": "android",
      "version": "3.0.0",
      "description": "Android开发工具集",
      "commands_count": 12,
      "status": "active",
      "path": "/Users/user/.globalscripts/plugins/android"
    },
    {
      "name": "git", 
      "version": "3.0.0",
      "description": "Git工作流工具",
      "commands_count": 8,
      "status": "active",
      "path": "/Users/user/.globalscripts/plugins/git"
    },
    {
      "name": "system",
      "version": "3.0.0", 
      "description": "系统管理工具",
      "commands_count": 6,
      "status": "active",
      "path": "/Users/user/.globalscripts/plugins/system"
    }
  ],
  "summary": {
    "total_enabled": 3,
    "total_commands": 26
  }
}
```

##### 3.2 gs-plugins info - 显示插件详细信息
```bash
# 基本用法
gs-plugins info <插件名>          # 显示插件详细信息
gs-plugins info <插件名> --json   # JSON格式输出
gs-plugins info <插件名> --commands # 显示插件命令列表

# 输出示例
$ gs-plugins info android
Global Scripts V3 - 插件信息
============================

📱 Android开发工具集
├─ 插件名称: android
├─ 版本号: 3.0.0
├─ 状态: ✅ 已启用
├─ 类型: 主插件
├─ 安装路径: /Users/user/.globalscripts/plugins/android
├─ 配置文件: android.meta
├─ 最后加载: 2025-05-18 14:30:25
└─ 内存占用: 1.2MB

📋 插件描述:
提供完整的Android开发工具集，包括ADB工具、设备管理、
编译脚本、日志查看等功能，支持多设备并行操作。

🏗️ 架构信息:
├─ 主插件: android.sh (gs_android_* 函数)
├─ 子模块: adb, fastboot, logcat, build
├─ 依赖检查: ✅ ADB已安装, ✅ Fastboot已安装
└─ 系统依赖: Android SDK, Platform Tools

📝 可用命令 (12个):
设备管理:
  gs-android-device-list        列出所有设备
  gs-android-device-info        显示设备信息  
  gs-android-device-screenshot  设备截图

ADB工具:
  gs-android-adb-shell         ADB Shell连接
  gs-android-adb-install       APK安装
  gs-android-adb-push          文件推送到设备
  gs-android-adb-pull          从设备拉取文件

日志工具:
  gs-android-logcat            查看设备日志
  gs-android-logcat-clear      清空设备日志

构建工具:
  gs-android-build-debug       构建Debug版本
  gs-android-build-release     构建Release版本
  gs-android-build-clean       清理构建文件

⚙️ 配置信息:
├─ Android SDK: /opt/android-sdk
├─ 默认设备: auto (自动选择)
├─ ADB超时: 30秒
└─ 自动检测设备: 已启用

📊 使用统计:
├─ 最常用命令: gs-android-adb-shell (45%)
├─ 总调用次数: 127次
└─ 平均响应时间: 1.8ms

# JSON格式输出
$ gs-plugins info android --json
{
  "name": "android",
  "version": "3.0.0",
  "status": "enabled",
  "type": "main_plugin",
  "path": "/Users/user/.globalscripts/plugins/android",
  "config_file": "android.meta",
  "last_loaded": "2025-05-18T14:30:25Z",
  "memory_usage_mb": 1.2,
  "description": "提供完整的Android开发工具集...",
  "architecture": {
    "main_plugin": "android.sh",
    "submodules": ["adb", "fastboot", "logcat", "build"],
    "dependencies_status": {
      "adb": "installed",
      "fastboot": "installed"
    },
    "system_dependencies": ["Android SDK", "Platform Tools"]
  },
  "commands": [
    {
      "name": "gs-android-device-list",
      "description": "列出所有设备",
      "category": "设备管理"
    }
    // ... 其他命令
  ],
  "configuration": {
    "android_sdk_path": "/opt/android-sdk",
    "default_device": "auto",
    "adb_timeout": 30,
    "auto_detect_devices": true
  },
  "statistics": {
    "most_used_command": "gs-android-adb-shell",
    "total_calls": 127,
    "avg_response_time_ms": 1.8
  }
}
```

##### 3.3 gs-plugins enable - 启用插件
```bash
# 基本用法
gs-plugins enable <插件名>               # 启用指定插件
gs-plugins enable <插件名> --force      # 强制启用（忽略依赖检查）
gs-plugins enable <插件名> --verbose    # 详细输出启用过程

# 输出示例
$ gs-plugins enable themes
启用插件: themes
================

🔍 检查插件状态...
  ├─ 插件存在: ✅ /Users/user/.globalscripts/plugins/themes
  ├─ 配置文件: ✅ themes.meta
  └─ 依赖检查: ✅ 无外部依赖

📦 加载插件文件...
  ├─ 主插件: ✅ themes.sh
  ├─ 子模块: ✅ zsh.sh, tmux.sh, vim.sh
  └─ 函数检测: ✅ 发现 4个 gs_themes_* 函数

🔗 注册命令别名...
  ├─ gs-themes-zsh         → gs_themes_zsh
  ├─ gs-themes-tmux        → gs_themes_tmux
  ├─ gs-themes-vim         → gs_themes_vim
  └─ gs-themes-status      → gs_themes_status

✅ 插件 'themes' 已成功启用
📋 新增 4个 命令可用
💡 使用 'gs-plugins info themes' 查看详细信息

$ gs-plugins enable nonexistent
❌ 错误: 插件 'nonexistent' 不存在
💡 使用 'gs-plugins list --available' 查看可用插件
```

##### 3.4 gs-plugins disable - 禁用插件
```bash
# 基本用法
gs-plugins disable <插件名>             # 禁用指定插件
gs-plugins disable <插件名> --force    # 强制禁用
gs-plugins disable <插件名> --keep-config # 保留配置文件

# 输出示例
$ gs-plugins disable themes
禁用插件: themes
================

🔍 检查插件状态...
  └─ 当前状态: ✅ 已启用

🗑️ 移除命令别名...
  ├─ gs-themes-zsh         ✅ 已移除
  ├─ gs-themes-tmux        ✅ 已移除  
  ├─ gs-themes-vim         ✅ 已移除
  └─ gs-themes-status      ✅ 已移除

📦 卸载插件函数...
  └─ 清理内存中的插件函数 ✅

⚙️ 配置处理...
  └─ 保留配置文件 (使用 --remove-config 可删除)

✅ 插件 'themes' 已成功禁用
📋 移除了 4个 命令
💡 使用 'gs-plugins enable themes' 可重新启用
```

##### 3.5 gs-plugins reload - 重新加载插件
```bash
# 基本用法
gs-plugins reload <插件名>             # 重新加载指定插件
gs-plugins reload <插件名> --force     # 强制重新加载
gs-plugins reload --all                # 重新加载所有插件

# 输出示例
$ gs-plugins reload android
重新加载插件: android
====================

🔄 执行重新加载流程...

1️⃣ 禁用当前插件...
  ├─ 移除命令别名: ✅ 12个命令
  ├─ 清理插件函数: ✅ 完成
  └─ 释放内存: ✅ 1.2MB

2️⃣ 重新检测插件...
  ├─ 扫描插件目录: ✅ android/
  ├─ 解析.meta文件: ✅ android.meta
  └─ 检查文件更新: ✅ 发现 2个文件更新

3️⃣ 重新加载插件...
  ├─ 加载主插件: ✅ android.sh  
  ├─ 加载子模块: ✅ adb.sh, fastboot.sh, logcat.sh, build.sh
  ├─ 函数检测: ✅ 发现 12个 gs_android_* 函数  
  └─ 注册命令: ✅ 12个命令别名

4️⃣ 验证插件状态...
  ├─ 命令可用性: ✅ 全部正常
  ├─ 依赖检查: ✅ ADB/Fastboot可用
  └─ 内存使用: ✅ 1.3MB (+0.1MB)

✅ 插件 'android' 重新加载完成
🔧 检测到代码更新并成功应用
📊 内存占用: 1.3MB | 命令数: 12个 | 耗时: 45ms

# 重新加载所有插件
$ gs-plugins reload --all
重新加载所有插件
================

🔄 正在重新加载 3个已启用插件...

android: ✅ 完成 (45ms)
git:     ✅ 完成 (32ms)  
system:  ✅ 完成 (28ms)

✅ 全部插件重新加载完成
📊 总耗时: 105ms | 总命令数: 26个 | 内存使用: 3.8MB
```

#### 实现要点
- 基于.meta文件的插件管理
- 支持插件状态跟踪和统计
- 命令别名的动态注册/注销
- 插件依赖检查和验证
- 内存使用监控
- 详细的错误处理和用户反馈

## 🔧 实现架构

### 输出格式技术实现

#### 1. JSON输出格式实现
系统命令支持`--json`格式输出，采用纯Shell实现，无需外部依赖：

```bash
# JSON输出核心实现
_gs_json_output() {
    local data="$1"
    # 可选依赖jq进行格式化（非必需）
    echo "$data" | jq '.' 2>/dev/null || echo "$data"
}

# 系统命令JSON输出示例
gs_system_version() {
    local format="${1:-text}"
    
    if [[ "$format" == "--json" ]]; then
        # 使用HERE document构建JSON结构
        cat << EOF
{
  "version": "3.0.0",
  "build_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "shell_type": "$(_gs_detect_shell_basic)",
  "system": "$(uname -s)",
  "components": {
    "core_modules": 6,
    "system_commands": 4,
    "plugins_loaded": $(_gs_count_loaded_plugins)
  },
  "performance": {
    "startup_time_ms": $(_gs_get_startup_time),
    "memory_usage_mb": $(_gs_get_memory_usage)
  }
}
EOF
    else
        # 标准文本输出
        echo "Global Scripts V3.0.0"
        echo "Build: $(date)"
        echo "Shell: $(_gs_detect_shell_basic)"
    fi
}
```

**技术要点：**
- ✅ **零依赖核心**：纯Shell字符串拼接实现基础JSON
- ✅ **可选增强**：支持`jq`工具格式化（非必需）
- ✅ **HERE document**：使用`cat << EOF`构建复杂JSON结构
- ✅ **动态数据**：支持函数调用和变量替换
- ✅ **错误处理**：jq不可用时回退到原始输出

#### 2. 表格输出格式实现
插件列表等命令支持美观的表格显示，采用纯Shell实现，无需外部工具：

```bash
# 表格绘制核心函数
_gs_draw_table() {
    local -a headers=("$@")
    local col_count=${#headers[@]}
    
    # 动态计算列宽（基于内容长度）
    local -a col_widths
    for i in "${!headers[@]}"; do
        col_widths[i]=${#headers[i]}
    done
    
    # 绘制表格顶部边框
    _gs_draw_border "top" "${col_widths[@]}"
    
    # 绘制表头行
    _gs_draw_row "${headers[@]}"
    
    # 绘制表头分隔线
    _gs_draw_border "middle" "${col_widths[@]}"
}

# 边框绘制函数
_gs_draw_border() {
    local type="$1"
    shift
    local -a widths=("$@")
    
    case "$type" in
        "top")
            printf "┌"
            for i in "${!widths[@]}"; do
                printf "%*s" $((widths[i] + 2)) "" | tr ' ' '─'
                [[ $i -lt $((${#widths[@]} - 1)) ]] && printf "┬"
            done
            printf "┐\n"
            ;;
        "middle")
            printf "├"
            for i in "${!widths[@]}"; do
                printf "%*s" $((widths[i] + 2)) "" | tr ' ' '─'
                [[ $i -lt $((${#widths[@]} - 1)) ]] && printf "┼"
            done
            printf "┤\n"
            ;;
        "bottom")
            printf "└"
            for i in "${!widths[@]}"; do
                printf "%*s" $((widths[i] + 2)) "" | tr ' ' '─'
                [[ $i -lt $((${#widths[@]} - 1)) ]] && printf "┴"
            done
            printf "┘\n"
            ;;
    esac
}

# 表格行绘制函数
_gs_draw_row() {
    local -a cells=("$@")
    printf "│"
    for cell in "${cells[@]}"; do
        printf " %-s │" "$cell"
    done
    printf "\n"
}

# 插件列表表格应用示例
gs_system_plugins_list() {
    local format="${1:-table}"
    
    if [[ "$format" == "--json" ]]; then
        # JSON格式输出
        _gs_plugins_json_output
        return
    fi
    
    # 表格模式输出
    echo "Global Scripts V3 - 插件列表"
    echo "============================"
    echo
    echo "✅ 已启用插件 ($(count_enabled_plugins)个):"
    
    # 绘制表格头
    _gs_draw_table "插件名称" "版本" "描述" "命令数量" "状态"
    
    # 绘制数据行
    while IFS=$'\t' read -r name version desc commands status; do
        _gs_draw_row "$name" "$version" "$desc" "$commands" "$status"
    done < <(_gs_get_enabled_plugins_data)
    
    # 绘制表格底部
    _gs_draw_border "bottom" 10 9 18 12 10
    
    echo
    echo "📊 统计信息:"
    echo "总插件数: $(count_total_plugins)个 | 已启用: $(count_enabled_plugins)个 | 总命令数: $(count_total_commands)个"
}
```

**技术优势：**
- ✅ **零外部依赖**：纯Shell实现，无需column、awk等工具
- ✅ **Unicode表格**：使用Box Drawing字符（┌┬┐├┼┤└┴┘│─）绘制专业边框
- ✅ **自适应宽度**：根据内容长度自动调整列宽
- ✅ **跨平台兼容**：Bash/Zsh均支持，macOS/Linux通用
- ✅ **彩色支持**：可集成ANSI转义序列实现彩色输出
- ✅ **性能优化**：直接printf输出，无额外进程开销

**实现细节：**
1. **Unicode Box Drawing**：使用标准Unicode字符绘制表格边框
2. **Printf格式化**：利用`printf`的对齐和宽度控制功能
3. **数组循环**：通过Shell数组处理表格数据结构
4. **动态计算**：根据实际内容自动调整列宽度
5. **模块化设计**：边框、行、数据分离，便于复用

这种纯Shell实现方案确保了Global Scripts V3系统命令的专业输出效果，同时保持了零依赖的架构优势。

### 系统命令的统一架构
```bash
# 系统命令实现结构
system/
├── help/
│   ├── help.meta           # 帮助系统元数据
│   └── help.sh            # gs_system_help_* 函数实现
├── version/
│   ├── version.meta        # 版本管理元数据  
│   └── version.sh         # gs_system_version_* 函数实现
├── status/ 
│   ├── status.meta        # 状态检查元数据
│   └── status.sh          # gs_system_status_* 函数实现
└── plugins/
    ├── plugins.meta       # 插件管理元数据
    └── plugins.sh         # gs_system_plugins_* 函数实现
```

### 命令注册机制
```bash
# 自动检测和注册系统命令
gs_system_version() { ... }           # → gs-version
gs_system_status() { ... }            # → gs-status  
gs_system_plugins_list() { ... }      # → gs-plugins list
gs_system_plugins_info() { ... }      # → gs-plugins info
gs_system_plugins_enable() { ... }    # → gs-plugins enable
gs_system_plugins_disable() { ... }   # → gs-plugins disable
gs_system_plugins_reload() { ... }    # → gs-plugins reload
```

这些系统命令为Global Scripts V3提供了完整的管理和监控能力，确保用户能够有效地管理和使用整个系统。