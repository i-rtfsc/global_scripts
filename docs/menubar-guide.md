# macOS 菜单栏状态监控指南

**中文** | [English](./en/menubar-guide.md)

## 概览

macOS 菜单栏状态监控提供了从 macOS 菜单栏直接查看 Global Scripts 命令执行状态的实时可见性。当在终端运行 GS 命令时，您可以在不保持终端窗口可见的情况下查看进度、状态和系统指标。

**平台**: 仅支持 macOS 10.10+

**依赖**:
- `rumps>=0.4.0` (macOS 菜单栏框架)
- `psutil>=5.9.0` (系统指标)
- `aiohttp>=3.8.0` (异步 HTTP 客户端，用于一言 API)

---

## 功能特性

### 命令状态显示

- **自动启动**: 运行 GS 命令时菜单栏应用自动启动
- **实时进度**: 显示命令名称、进度百分比和已用时间
- **完成状态**: 显示成功 (✓) 或失败 (✗) 及耗时
- **状态清除**: 5 秒后自动清除回到空闲状态
- **跑马灯效果**: 文本过长时自动滚动显示（支持中英文智能宽度计算）

### 系统指标监控

- **CPU 温度**: 显示当前 CPU 温度
  - **Apple Silicon (M1/M2/M3/M4/M5)**: 使用 3 层检测方法
    1. `sysctl` 热级别
    2. `ioreg` 设备温度
    3. CPU 使用率估算
- **内存使用**: 显示内存使用百分比
- **自动刷新**: 每 5 秒更新一次

### 显示格式

#### 状态栏标题显示
- **空闲**: `GS`
- **运行中（无进度）**: `GS version 2s`
- **运行中（有进度）**: `GS android.adb 45% 2m15s`
- **运行中（带输出）**: `GS spider.crawl 50% 1m 找到10条` (自动滚动)
- **成功**: `GS version 1.2s ✓`
- **失败**: `GS command 0.5s ✗`

#### 下拉菜单显示
点击菜单栏图标查看：
```
┌────────────────────────┐
│ CPU: 52°C              │  ← 实时 CPU 温度
│ Memory: 45%            │  ← 实时内存使用率
├────────────────────────┤
│ Quit                   │  ← 退出应用
└────────────────────────┘
```

---

## 安装配置

### 1. 安装依赖

```bash
uv sync
```

### 2. 启用菜单栏

编辑 `~/.config/global-scripts/config/gs.json`:

```json
{
  "menubar": {
    "enabled": true,
    "refresh_interval": 5,
    "show_cpu_temp": true,
    "show_memory": true,
    "sentence_type": "一言",
    "sentence_refresh_interval": 300,
    "marquee_update_interval": 0.3
  }
}
```

### 3. 运行命令自动启动

```bash
gs version
```

菜单栏应用将自动在后台启动。

---

## 配置选项

### 基础配置

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enabled` | boolean | `false` | 启用/禁用菜单栏功能 |
| `refresh_interval` | number | `5` | 系统指标刷新间隔（秒） |
| `show_cpu_temp` | boolean | `true` | 在菜单中显示 CPU 温度 |
| `show_memory` | boolean | `true` | 在菜单中显示内存使用率 |

### 高级配置

| 选项 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `sentence_type` | string | `"一言"` | 一言类型（一言/毒鸡汤/社会语录/舔狗日记/诗词）|
| `sentence_refresh_interval` | number | `300` | 一言刷新间隔（秒，默认 5 分钟）|
| `marquee_update_interval` | number | `0.3` | 跑马灯滚动速度（秒）|

### 配置示例

**最小配置**:
```json
{
  "menubar": {
    "enabled": true
  }
}
```

**完整配置**:
```json
{
  "menubar": {
    "enabled": true,
    "refresh_interval": 10,
    "show_cpu_temp": true,
    "show_memory": true,
    "sentence_type": "诗词",
    "sentence_refresh_interval": 600,
    "marquee_update_interval": 0.2
  }
}
```

---

## 使用说明

### 基本用法

菜单栏一旦启用，将自动工作：

1. **首次运行命令触发自动启动**:
   ```bash
   gs system doctor
   ```
   → 菜单栏应用在后台启动

2. **后续命令更新状态**:
   ```bash
   gs android build aosp
   ```
   → 菜单栏显示: `GS android.build 0m30s`

3. **查看下拉菜单**（点击菜单栏图标）:
   ```
   CPU: 52°C
   Memory: 45%
   ─────────────
   Quit
   ```

### 手动控制

```bash
# 启动菜单栏
gs menubar start

# 停止菜单栏
gs menubar stop

# 重启菜单栏
gs menubar restart

# 查看状态
gs menubar status
```

---

## 状态转换流程

```
空闲 → 运行（无进度）→ 运行（有进度）→ 成功/失败 → [5秒] → 空闲
 GS      GS cmd 5s      GS cmd 45% 2m      GS cmd 1s ✓/✗     GS
```

### 详细说明

#### 1. 空闲状态
- **显示**: `GS`
- **触发条件**: 无命令在执行
- **持续时间**: 直到下一个命令开始

#### 2. 运行状态（无进度）
- **显示**: `GS version 5s`
- **触发条件**: 命令开始执行，但没有进度报告
- **实时更新**: 时间每秒刷新

#### 3. 运行状态（有进度）
- **显示**: `GS spider.crawl 45% 2m15s`
- **触发条件**: 命令执行中，且通过 generator yield 报告进度
- **进度范围**: 0-100%

**示例插件代码**:
```python
@plugin_function
async def long_task(self, args=None):
    for i in range(100):
        # 执行任务...
        yield {"progress": i}  # 报告进度
    return CommandResult(success=True)
```

#### 4. 成功状态
- **显示**: `GS version 1.2s ✓`
- **触发条件**: 命令执行成功
- **持续时间**: 显示 5 秒后自动清除

#### 5. 失败状态
- **显示**: `GS command 0.5s ✗`
- **触发条件**: 命令执行失败或异常
- **持续时间**: 显示 5 秒后自动清除

---

## 跑马灯功能

### 触发条件

文本显示宽度超过 30 时自动启用跑马灯：
- **中文字符**: 占 2 个宽度
- **英文/数字/符号**: 占 1 个宽度

**示例**:
- `GS version 1s` → 宽度 13，不滚动
- `GS 这是一个比较长的句子` → 宽度 > 30，滚动显示
- `GS This is a very long sentence` → 宽度 > 30，滚动显示

### 配置

```json
{
  "menubar": {
    "marquee_update_interval": 0.3  // 滚动速度（秒）
  }
}
```

---

## CPU 温度监控

### Apple Silicon (M1/M2/M3/M4/M5)

使用 3 层回退检测方法：

#### 方法 1: sysctl（热级别）
```bash
sysctl machdep.xcpm.cpu_thermal_level
# 返回: 0-100 的热级别
# 转换: 40 + (level * 0.6) = 温度（°C）
```
- **优点**: 快速，无需权限
- **缺点**: 热级别近似值

#### 方法 2: ioreg（设备温度）
```bash
ioreg -rn AppleARMIODevice | grep temperature
# 返回: 温度值（通常是 °C * 100）
# 转换: temp_raw / 100.0
```
- **优点**: 较准确
- **缺点**: 部分芯片可能不可用

#### 方法 3: CPU 使用率估算
```python
cpu_percent = psutil.cpu_percent()
estimated_temp = 40 + (cpu_percent * 0.4)
# 0% CPU = 40°C，100% CPU = 80°C
```
- **优点**: 始终可用
- **缺点**: 仅为估算值

### Intel Mac

使用 `psutil.sensors_temperatures()` 读取传感器数据。

---

## IPC 通信机制

### 通信协议

- **协议**: Unix Domain Socket
- **路径**: `~/.config/global-scripts/menubar.sock`
- **方向**: CLI → 菜单栏应用（单向）

### 消息类型

#### 1. 命令开始（command_start）
```json
{
  "type": "command_start",
  "command": "android.adb.devices",
  "timestamp": 1699459200.123
}
```

#### 2. 进度更新（progress_update）
```json
{
  "type": "progress_update",
  "percentage": 45,
  "elapsed": 15.3,
  "output": "已处理 45 个文件"
}
```

#### 3. 命令完成（command_complete）
```json
{
  "type": "command_complete",
  "success": true,
  "duration": 1.23,
  "output": "找到 3 个设备",
  "error": null
}
```

---

## 故障排查

### CPU 温度显示 N/A

```bash
# 测试检测方法
# 方法 1: sysctl
sysctl machdep.xcpm.cpu_thermal_level

# 方法 2: ioreg
ioreg -rn AppleARMIODevice | grep temperature

# 方法 3: CPU 使用率
uv run python -c "
import psutil
cpu = psutil.cpu_percent(interval=0.5)
temp = 40 + (cpu * 0.4)
print(f'CPU: {cpu}% → 估算温度: {temp:.1f}°C')
"

# 查看日志
tail -100 ~/.config/global-scripts/logs/menubar.log | grep -i temp
```

### 菜单栏未自动启动

```bash
# 检查配置
cat ~/.config/global-scripts/config/gs.json | grep -A4 menubar

# 查看日志
tail -50 ~/.config/global-scripts/logs/gs.log | grep -i menubar

# 手动启动测试
gs menubar start
```

### 应用显示在 Dock 中

```bash
# 检查激活策略
tail -20 ~/.config/global-scripts/logs/menubar.log | grep activation
# 应该看到: "Set NSApplicationActivationPolicyAccessory (menu bar only)"

# 重启菜单栏
gs menubar stop
sleep 2
gs menubar start
```

### 跑马灯不滚动

检查文本宽度是否超过 30：
```bash
uv run python -c "
from gscripts.menubar.marquee import Marquee
text = 'GS 您的文本内容'
m = Marquee(text, max_length=30)
print(f'文本: {text}')
print(f'宽度: {m._display_width}')
print(f'需要滚动: {m.needs_scroll}')
"
```

---

## 日志和调试

### 日志文件

```bash
~/.config/global-scripts/logs/menubar.log
```

### 实时查看日志

```bash
# 实时查看
tail -f ~/.config/global-scripts/logs/menubar.log

# 查看最近 50 行
tail -50 ~/.config/global-scripts/logs/menubar.log

# 过滤特定内容
tail -100 ~/.config/global-scripts/logs/menubar.log | grep -i "command"
```

---

## 技术实现

### 架构组件

| 组件 | 文件 | 说明 |
|------|------|------|
| 主应用 | `menubar/app.py` | rumps 菜单栏应用主类 |
| 状态管理 | `menubar/status_manager.py` | 命令状态格式化 |
| 系统监控 | `menubar/monitors.py` | CPU/内存监控 |
| IPC 服务器 | `menubar/ipc.py` | Unix socket 服务器 |
| 跑马灯 | `menubar/marquee.py` | 滚动文本效果 |
| 一言 API | `menubar/sentence_api.py` | 随机句子获取 |
| 图标 | `menubar/icon.py` | 菜单栏图标定义 |

### 后台任务

1. **系统指标更新器**: 每 5 秒更新 CPU 温度和内存使用率
2. **一言更新器**: 每 5 分钟获取新的随机句子（可配置）
3. **跑马灯更新器**: 每 0.3 秒更新滚动位置（如需滚动）
4. **IPC 服务器**: 监听来自 CLI 的命令状态消息

---

## 特性对比

### 与 ClashX 风格对比

| 特性 | GS 菜单栏 | ClashX | 说明 |
|------|----------|--------|------|
| 仅菜单栏显示 | ✅ | ✅ | 不在 Dock 显示 |
| Cmd+Tab 隐藏 | ✅ | ✅ | 不在应用切换器显示 |
| 实时状态更新 | ✅ | ✅ | 命令执行状态 |
| 系统监控 | ✅ CPU/内存 | ✅ 网速/延迟 | 不同监控内容 |
| 文本显示 | ✅ | ✅ | 无 emoji 图标 |
| 深色模式适配 | ✅ | ✅ | 自动适配系统主题 |
| 跑马灯效果 | ✅ | - | GS 独有功能 |

---

## 最佳实践

### 1. 配置优化

**性能优先**:
```json
{
  "menubar": {
    "enabled": true,
    "refresh_interval": 10,
    "marquee_update_interval": 0.5
  }
}
```

**视觉优先**:
```json
{
  "menubar": {
    "enabled": true,
    "refresh_interval": 3,
    "marquee_update_interval": 0.2
  }
}
```

### 2. 使用场景

- **长时间命令**: 了解进度和剩余时间
- **后台执行**: 命令在后台运行时保持状态可见
- **系统监控**: 实时查看 CPU 温度和内存使用
- **多任务工作**: 无需切换终端窗口即可查看状态

### 3. 性能建议

- `refresh_interval` 设置 5-10 秒即可
- `marquee_update_interval` 建议 0.2-0.5 秒
- `sentence_refresh_interval` 建议 5-10 分钟

---

## 限制和注意事项

1. **仅支持 macOS**: 菜单栏功能仅在 macOS 上可用
2. **需要 rumps**: 必须安装 `rumps` 库
3. **Apple Silicon 温度**: 温度读取为近似值，非精确测量
4. **文本长度**: 超长文本会启用跑马灯，可能影响可读性
5. **状态清除**: 5 秒后自动清除，如需更长时间请修改配置

---

## 常见问题 (FAQ)

### Q: 如何更改菜单栏图标？

A: 当前使用 "GS" 文字图标。未来版本将支持自定义图标文件。可以临时修改 `src/gscripts/menubar/icon.py` 中的 `MENUBAR_ICON` 变量。

### Q: 为什么空闲状态不显示一言？

A: 为避免挤掉其他菜单栏图标，空闲状态仅显示 "GS"。一言功能已实现但默认禁用。

### Q: 如何延长状态显示时间？

A: 修改 `src/gscripts/menubar/app.py` 中的 `threading.Timer(5.0, self._clear_status)` 参数（当前为 5 秒）。

### Q: 跑马灯滚动太快/太慢？

A: 在配置中调整 `marquee_update_interval` 参数（默认 0.3 秒）。

### Q: 支持 Windows/Linux 吗？

A: 不支持。菜单栏功能仅限 macOS。其他平台可以考虑使用系统托盘实现。

---

## 更新日志

### 版本 5.2.0-dev
- ✨ 新增跑马灯滚动效果
- ✨ 新增一言 API 集成
- ✨ 支持命令输出显示
- ✨ 优化 Apple Silicon CPU 温度检测
- 🐛 修复 Dock 图标显示问题
- 🐛 修复状态清除时间（15s → 5s）
- 🎨 图标改为 "GS" 文字（可配置）

---

## 相关资源

- [GS 主文档](../README.md)
- [插件开发指南](./plugin-development.md)
- [架构文档](./architecture.md)
- [贡献指南](./contributing.md)
