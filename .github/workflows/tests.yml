name: Tests and Coverage

on:
  push:
    branches: [main, develop, feature/**]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.8', '3.9', '3.10', '3.11']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Compile GS6 Python plugins
        run: |
          python -m py_compile plugins/*/*.py plugins/*/*/*.py

      - name: Run unit tests
        run: |
          uv run pytest tests/unit/ -v -m "not slow" --tb=short

      - name: Run integration tests
        run: |
          uv run pytest tests/integration/ -v --tb=short

      - name: Run E2E tests
        run: |
          uv run pytest tests/e2e/ -v --tb=short

      - name: Run Rust core tests
        working-directory: rust
        run: cargo test --workspace

      - name: Show Rust toolchain
        run: rustc --version

      - name: Check Rust formatting
        run: cargo fmt --manifest-path rust/Cargo.toml --all -- --check

      - name: Check repository diff whitespace
        run: git diff --check

      - name: Check generated artifacts are ignored
        run: |
          test -z "$(git ls-files --others --exclude-standard -- '*.pyc' '*.coverage' 'htmlcov/*' 'coverage.xml')"

      - name: Verify GS 6.0 development front door
        run: |
          bash -n scripts/verify_gs6.sh
          cargo build --manifest-path rust/Cargo.toml -p gs
          cargo build --release --manifest-path rust/Cargo.toml -p gs
          bash scripts/stage_gs6_release.sh
          bash scripts/verify_gs6.sh

      - name: Run Python plugin SDK self-test
        run: python sdk/python/selftest.py

  coverage:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run tests with coverage
        run: |
          uv run pytest tests/ -v -m "not slow" \
            --cov=src/gscripts \
            --cov-report=xml \
            --cov-report=html \
            --cov-report=term-missing \
            --cov-fail-under=50

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Archive coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            htmlcov/
            coverage.xml
          retention-days: 30

      - name: Coverage comment
        if: github.event_name == 'pull_request'
        run: |
          echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          uv run pytest tests/ -v -m "not slow" \
            --cov=src/gscripts \
            --cov-report=term | grep -A 20 "TOTAL" >> $GITHUB_STEP_SUMMARY

  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run Ruff linting
        run: |
          uv run ruff check src/ tests/

      - name: Run Black formatting check
        run: |
          uv run black --check src/ tests/

      - name: Run MyPy type checking
        run: |
          uv run mypy src/ --ignore-missing-imports
        continue-on-error: true

  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install UV
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run security tests
        run: |
          uv run pytest tests/unit/security/ -v --tb=short

      - name: Check for known vulnerabilities
        run: |
          pip install safety
          safety check --json || true
        continue-on-error: true
